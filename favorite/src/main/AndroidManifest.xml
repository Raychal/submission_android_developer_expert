<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution">

    <dist:module
        dist:instant="false"
        dist:title="@string/favorite">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity
            android:name="com.raychal.favorite.ui.FavoriteActivity"
            android:exported="true"
            android:label="Favorite Games"
            android:theme="@style/Theme.SubmissionAndroidDeveloperExpert">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="submissionandroiddeveloperexpert"
                    android:host="favorites"/>
            </intent-filter>
        </activity>
    </application>
</manifest>